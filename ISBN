class ISBN 
{
	public static void main(String[] args) 
	{
		int n=1234;
		int count=0;
	    int sum=0;//4
		while(n>0)
		{
			int ld=n%10;//4
			count++;//1*4
			sum=sum+count*ld;
			n=n/10;
		}
		System.out.println(sum);

	}
}
